const starterCode = `function trap(height: number[]): number {
  let left = 0;
  let right = height.length - 1;
  let leftMax = 0;
  let rightMax = 0;
  let water = 0;

  while (left <= right) {
     // Your Logic Here (Two Pointer)
     
  }
  
  return water;
};`;

export default function TrappingRainWaterCodeEditor() {
    return null;
}

export { starterCode };

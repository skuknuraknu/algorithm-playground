const starterCode = `function uniquePaths(m: number, n: number): number {
  // Buat array 2D atau gunakan trik kombinatorik
  // dp[i][j] = dp[i-1][j] + dp[i][j-1]
  
  return 0;
};`;

export default function UniquePathsCodeEditor() {
    return null;
}

export { starterCode };
